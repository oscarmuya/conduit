spring:
  application:
    name: conduit

  datasource:
    url: jdbc:postgresql://localhost:5432/conduit_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

server:
  port: 3000
